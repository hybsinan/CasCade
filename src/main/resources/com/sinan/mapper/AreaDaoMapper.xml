<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.sinan.dao.AreaDao">
    <select id="selectAreaByCityCode" resultType="com.sinan.entity.Area">
        select  * from area where citycode =#{citycode}
    </select>
    <update id="">
        update comment set count =#{count}
    </update>
    <select id=""
</mapper>
